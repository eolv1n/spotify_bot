🎧 Spotify Bot — DevOps / Python практика

🎯 Telegram-бот для обработки Spotify-ссылок и конвертации треков.
⚙️ CI/CD: GitHub Actions + systemd + Docker + автодеплой на VPS.
🧪 Тесты: pytest, coverage, артефакты и отчёты.
🚀 DevOps через практику: Linux, Bash, Git, Docker.
📦 Репозиторий: github.com/eolv1n/spotify_bot

💬 Telegram: @eolv1n


🚀 DevOps / Python Engineer — eolv1n
💡 О себе

Изучаю DevOps и Linux-администрирование через практику: веду собственный Telegram-бот (Spotify Bot), автоматизирую деплой и сборку окружений через systemd и Docker, развиваю CI/CD через GitHub Actions.

🧠 Основные навыки
Область	Навыки
Linux	systemd, пользователи, сеть, права, cron, логи
Bash	автоматизация, бэкапы, анализ, условия
Git	ветвление, SSH, CI-логика dev/main
Docker	сборка образов, docker-compose, сети, volumes
Python	Telegram Bot API, Spotify API, обработка данных
DevOps	деплой, мониторинг, резервирование, оптимизация
⚙️ Практический проект — Spotify Bot

Репозиторий: github.com/eolv1n/spotify_bot

Функциональность:

Обработка Spotify-ссылок → карточка трека

Перевод ссылок на другие сервисы

Inline-режим и автоудаление сообщений

Автоматический деплой через systemd и GitHub Actions

Технологии: Python, Docker, Bash, Git, systemd
CI/CD-процесс: dev → тесты → main → деплой

🔁 CI/CD Workflow

Файл: .github/workflows/CI.yml

Этапы пайплайна:

Lint — проверка качества кода.

Test — юнит-тесты через pytest с отчётами (junitxml, coverage).

Build / Deploy — сборка и автоматический деплой на Debian VPS через deploy.sh.

Особенности:

Запуск при push в ветку main.

Управление зависимостями через needs: (build зависит от lint, deploy — от test).

Используется appleboy/ssh-action для деплоя по SSH.

Runner: ubuntu-latest.

Возможность подключения Allure-отчётов и smoke-тестов деплоя.

🧪 Тестирование

Unit-тесты: выполняются через pytest.

Отчётность:

report.xml (JUnit XML) — артефакт junit-report.

HTML-покрытие (pytest-cov) — артефакт coverage-report.

Пример структуры:

tests/
  unit/
    test_basic.py


Пример запуска тестов:

pytest -q --cov=app --cov-branch --junitxml=report.xml

🔐 Деплой

Используется GitHub Action:

- name: Deploy to Debian VPS
  uses: appleboy/ssh-action@v1.2.0
  with:
    host: ${{ secrets.SERVER_HOST }}
    username: ${{ secrets.SERVER_USER }}
    key: ${{ secrets.SSH_KEY }}
    script: |
      cd ~/spotify_bot
      ./deploy.sh


Secrets: SERVER_HOST, SERVER_USER, SSH_KEY.
Результат: после успешных тестов deploy.sh разворачивает новую версию бота на сервере.

🧭 Следующие шаги

 Углубить Python (функции, классы, API).

 Расширить pytest-тесты (integration + e2e).

 Добавить Allure-отчётность.

 Настроить smoke-тест деплоя.

 Перейти к Ansible и hardening (SSH, fail2ban, ufw).

📬 Контакты

📫 Telegram: @eolv1n

💻 GitHub: github.com/eolv1n

