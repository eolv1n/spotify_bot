[pytest]
addopts = -q --cov=. --cov-branch --cov-report=xml:coverage.xml --cov-report=html:htmlcov
testpaths = tests
pythonpath = .
  env =
    TELEGRAM_TOKEN=fake-token
    SPOTIFY_CLIENT_ID=fake-id
    SPOTIFY_CLIENT_SECRET=fake-secret
    DATABASE_URL=sqlite:///:memory: